# ProjectX_task1
the task is solved in phases. after getting the maze images at first they were cleaned(clean.py). even after intial cleaning small rectangles were visible. then it was fixed(cleaned.py) after that the walkable path was turned white(invert.py). at last bfs was applied (solve.py) and output was saved (Final Answer.txt). solved and unsolved images were stored in separate folders.
